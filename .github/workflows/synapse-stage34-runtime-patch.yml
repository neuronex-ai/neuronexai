# This temporary workflow is intentionally inert. The one-shot patch now runs
# from deploy-synapse.yml on the feature PR and this file is removed by that job.
name: Synapse stage 3/4 patch placeholder
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Patch is handled by deploy-synapse.yml"
